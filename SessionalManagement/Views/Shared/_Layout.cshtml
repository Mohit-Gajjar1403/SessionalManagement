@using Microsoft.AspNetCore.Http
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Sessional Management</title>

    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
</head>
<body class="d-flex flex-column min-vh-100">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">Sessional Management</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        @if (Context.Session.GetString("Username") != null)
                        {
                            var role = Context.Session.GetString("Role");
                            var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
                            var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
                            <li class="nav-item">
                                <a class="nav-link @(currentController == role && currentAction == "Index" ? "active" : "")" asp-controller = "@role.ToString()" asp-action="Index">Home</a>
                            </li>
                            if (role == "Student")
                            {
                                <li class="nav-item">
                                    <a class="nav-link @(currentController == "Student" && currentAction == "MyMarks" ? "active" : "")" asp-controller="Student" asp-action="MyMarks">My Marks</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link @(currentController == "Student" && currentAction == "MarksGraph" ? "active" : "")" asp-controller="Student" asp-action="MarksGraph">Performance Chart</a>
                                </li>
                            }

                            else if (role == "Teacher")
                            {
                                <li class="nav-item">
                                    <a class="nav-link @(currentController == "Teacher" && currentAction == "UploadMarks" ? "active" : "")" asp-controller="Teacher" asp-action="UploadMarks">Upload Marks</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link @(currentController == "Admin" && currentAction == "StudentDetails" ? "active" : "")" asp-controller="Admin" asp-action="StudentDetails"> Student Details</a>
                                </li>
                            }
                            else if (role == "Admin")
                            {
                                <li class="nav-item">
                                    <a class="nav-link @(currentController == "Admin" && currentAction== "StudentDetails" ? "active" : "")" asp-controller="Admin" asp-action="StudentDetails">Student Details</a>
                                </li>
                                <li>
                                    <a class="nav-link @(currentController == "Admin" && currentAction == "Details" ? "active" : "")" asp-controller="Admin" asp-action="Details">Teacher Details</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Admin" asp-action="ManageUsers">Add Teacher</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Admin" asp-action="ManageUsers">Add Subject</a>
                                </li>
							}
                        }
                    </ul>

                    <ul class="navbar-nav">
                        @if (Context.Session.GetString("Username") != null)
                        {
                            <li class="nav-item">
                                <span class="navbar-text me-2">Hello, @Context.Session.GetString("Username")!</span>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Account" asp-action="Logout">Logout</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="flex-fill container mt-4">
        @RenderBody()
    </main>

    <footer class="bg-dark text-white text-center py-3 mt-auto">
        <div class="container">
            <p>&copy; @DateTime.Now.Year - Sessional Management System</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
